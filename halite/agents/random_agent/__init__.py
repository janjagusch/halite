"""
Import the Agent object here so that it can be detected by the importer. 
It must have the name Agent (this can be avoided if we iterate through 
the classes in the module, but this is a much simpler solution.)
"""

from .agent import RandomAgent as Agent

